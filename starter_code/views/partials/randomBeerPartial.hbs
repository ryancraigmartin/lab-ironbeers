{{!-- <div class="random-beer-partial">
  <div class="partial-beer-image">
    <img src="{{this.image_url}}" alt="Beer Image">
  </div>
  <div class="partial-randombeer-name">
    <h3>{{this.name}}</h3>
  </div>
  <div class="partial-randombeer-tagline">
    <h4>{{this.tagline}}</h4>
  </div>
  <div class="partial-randombeer-description">
    <h4>{{this.description}}</h4>
  </div>
  <div class="partial-randombeer-food-pairing">
    <span>Food Pairing:</span>
      <ul>
        {{#each this.food_pairing}}
          <li>{{this}}</li>
        {{/each}}
      </ul>
  </div>
  <div class="partial-randombeer-brewers-tips">
    <span>Brewer's Tips:</span>
     <p>{{this.brewers_tips}}</p>
  </div>
</div> --}}
